query GetPokemon {
  pokemon_v2_pokemon(where: {name: {_is_null: false, _eq: "$name"}}) {
    name
    pokemon_v2_pokemonsprites(where: {sprites: {_has_keys_all: ["front_shiny", "front_default"], _is_null: false}}) {
      sprites(path: "other.official-artwork")
    }
  }
}
